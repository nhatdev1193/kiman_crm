<nav class="navbar navbar-static-top">
  <div class="container-fluid">
    <div class="navbar-header">
      <a href="<%= staff_dashboard_path %>" class="navbar-brand"><b>KimAn CRM</b></a>
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse">
        <i class="fa fa-bars"></i>
      </button>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse pull-left" id="navbar-collapse">
      <ul class="nav navbar-nav">
        <li class="active"><a href="#">Dashboard</a></li>
        <li><a href="<%= staff_people_path %>">Khách hàng</a></li>
        <li><a href="<%= staff_contracts_path %>">Hợp đồng</a></li>
      </ul>
    </div><!-- /.navbar-collapse -->

    <!-- Navbar Right Menu -->
    <div class="navbar-custom-menu">
      <ul class="nav navbar-nav">
        <li class="dropdown user user-menu">
          <!-- Menu Toggle Button -->
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">
            <i class="fa fa-user-circle"></i> Hi, <%= current_staff.name %>
          </a>
          <ul class="dropdown-menu">
            <li class="user-body">
              <span class="profile-username">
                <i class="fa fa-user-circle"></i> <%= current_staff.name %> - <%= current_staff.id %>
              </span>
            </li>
            <li class="user-body">
              <%= link_to edit_staff_registration_path do %>
                <%= fa_icon 'cogs' %> Cài đặt profile
              <% end %>
            </li>
            <li class="user-body">
              <%= link_to destroy_staff_session_path, method: :delete do %>
                <%= fa_icon 'sign-out' %> Đăng xuất
              <% end %>
            </li>
          </ul>
        </li>
      </ul>
    </div>
    <!-- /.navbar-custom-menu -->
  </div><!-- /.container-fluid -->
</nav>
